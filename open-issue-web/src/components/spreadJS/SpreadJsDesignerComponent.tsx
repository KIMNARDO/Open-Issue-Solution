import { Designer, DesignerProp } from '@grapecity/spread-sheets-designer-react';

const SpreadJsDesignerComponent = (props: DesignerProp) => {
  return <Designer {...props} />;
};

export default SpreadJsDesignerComponent;
